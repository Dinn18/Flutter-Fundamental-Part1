import 'package:flutter/material.dart';

class myImageWidget extends StatelessWidget {
  const myImageWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Image.asset("assets/poliwangi.jpg");
  }
}
